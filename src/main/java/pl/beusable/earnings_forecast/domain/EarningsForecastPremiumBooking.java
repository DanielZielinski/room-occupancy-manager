package pl.beusable.earnings_forecast.domain;

import pl.beusable.earnings_forecast.domain.model.RoomList;

interface EarningsForecastPremiumBooking {

    void bookPremiumRooms(RoomList roomList);
}
